import numpy as np 

# 测试使用Mac终端命令对文件进行一系列操作

print('hello word')

def functions(a, *b):
    '''加法运算'''
    sum = 0 
    sum += a 
    if b != null:
	sum += sum(b)
    return sum 

a = 130 
b = [1, 2, 3]

functions(a, b.append(9))

# 在master分支上提交一次更改
# 在master分支上的提交-3
